<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
     android:layout_height="match_parent"
    tools:context=".activities.ToolsActivity"
    android:id="@+id/page_Tools">

    <GridLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:columnCount="2"
        android:rowCount="3">

        <Button
            android:id="@+id/btn_awk"
            android:text="btn_awk" />

        <Button
            android:id="@+id/btn_newtermin"
            android:text="btn_newtermin" />

        <Button
            android:id="@+id/btn_newband"
            android:text="btn_newband" />

        <Button
            android:id="@+id/btn_newsong"
            android:text="btn_newsong" />

        <Button
            android:id="@+id/btn_newrec"
            android:text="btn_newrec" />

        <Button
            android:id="@+id/btn_newsinger"
            android:text="btn_newsinger" />
    </GridLayout>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#fff"
        app:layout_constraintBottom_toBottomOf="parent"
        app:menu="@menu/bottom_navigation_menu" />
</androidx.constraintlayout.widget.ConstraintLayout>